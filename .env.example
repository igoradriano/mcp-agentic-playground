# Required for OpenAI calls
OPENAI_API_KEY=your_openai_api_key

# Optional model override
OPENAI_MODEL=gpt-4o-mini

# Optional SSE URL for native MCP client
MCP_SSE_URL=http://localhost:8000/sse

# Optional SQL MCP overrides (defaults use public demo DB)
MCP_SQL_HOST=143.244.215.137
MCP_SQL_PORT=5432
MCP_SQL_DATABASE=novadrive
MCP_SQL_USER=etlreadonly
MCP_SQL_PASSWORD=novadrive376A@
